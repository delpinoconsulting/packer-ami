{
    "variables": {
        "aws_access_key": "",
        "aws_secret_key": "",
        "region":         "us-east-1"
    },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-east-1",
    "source_ami_filter": {
      "filters": {
      "virtualization-type": "hvm",
      "name": "Ubuntu Server 18*",
      "root-device-type": "ebs"
      },
      "most_recent": true
    },
    "instance_type": "t2.small",
    "ssh_username": "centos",
    "ami_name": "Base-Ubuntu {{timestamp}}",
    "tags": {
      "Name": "Base-CentOS"
  }
}
],
  "provisioners": [{
    "type": "shell",
    "inline": [
      "sleep 30",
      "sudo apt-get -y update",
      "sudo apt-get install -y ruby",
      "sudo apt-get install -y curl gnupg2",
      "sudo apt-get install -y libcurl libcurl-devel",
      "sudo apt-get install -y libxml2-devel",
      "sudo apt-get install -y postgresql-devel",
      "sudo apt-get install -y wget",
      "sudo apt-get install -y epel-release",
      "sudo apt-get install -y nginx",
      "cd /home/ubuntu",
      "wget https://aws-codedeploy-us-east-1.s3.amazonaws.com/latest/install",
      "chmod +x ./install",
      "sudo ./install auto",
      "sudo service codedeploy-agent start"
    ]
  }]
}
